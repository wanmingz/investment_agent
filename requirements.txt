# Streamlit Community Cloud + local dashboard dependencies.
# streamlit_app.py also adds src/ to sys.path so the package imports without editable install.
openai>=1.40.0
pydantic>=2.7.0
python-dotenv>=1.0.0
rich>=13.7.0
yfinance>=0.2.40
streamlit>=1.32.0
httpx>=0.27.0
sentence-transformers>=3.0.0
